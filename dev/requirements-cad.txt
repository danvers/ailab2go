# Only needed to regenerate the camera housing (hardware/case_elp48mp.py).
# The ready-made STLs are in hardware/stl/, so you can ignore this file
# unless you want to change dimensions.
#   bash setup/tools.sh case
trimesh>=4.0
manifold3d>=2.5     # the boolean engine trimesh calls
scipy>=1.11         # convex hulls (rounded corners)
networkx>=3.0       # mesh repair
matplotlib>=3.8     # preview renders, optional
