__pycache__/
*.pyc
.venv/
.venv-tools/
# generated print files — the ready-to-print PDFs in material/ ARE
# committed (teachers must not need Python); only the intermediate
# HTML the generators produce is ignored
material/poster/*.html
material/cards/*.html
.DS_Store
.claude
setup/deploy.local
~$*
.~lock.*#
